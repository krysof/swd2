# 《水浒传 2》C++ 重写工程

原始游戏的 2,315 个文件均已移到仓库根目录的 `game/`，本目录只放逆向记录和新写的代码。

## 当前可运行部分

- DOS MZ 可执行文件解析器（头、重定位、入口、附加数据）；
- `RSK/RS1..RS4/RAP/RRO` 的原版 64 KiB LZ/Huffman 解码器；
- 原始 `SWD2.EXE` 启动/调度状态机的等价 C++ 实现；
- DOS 资源路径规范化与资源目录统计；
- MAPA/MAPZ 世界数据库、地图实体、DE 平面精灵、CHNA 事件与 Big5 对话；
- SDL 单窗口前端，以及合入同一进程的 MEO、RPG、FIG、DEMO 模块；
- 自动测试直接使用 `game/` 原始数据校验启动、地图、事件、对话和模块切换。

这仍不是完整游戏成品，但四个主程序已没有退出占位。MEO 流程可运行；RPG 的全部
62 个事件调度号、地图切换、三格实体碰撞、拐角滑行、自动实体事件和原版代码窗口驱动的
实体游走已进入可移植 VM。商店、背包使用、装备交换、精确出售及存档数据提交均可运行；
opcode 14 金钱条、DE 过场、RI 事件音乐、VOC、调色板淡入淡出、逐帧延时、mode-X
地址抖动和坐标文字也已按机器码路径重写。DE 过场会分别跟踪 opcode 29/43 选择的
图块字典与 opcode 35 的 RAP 布局，因而可复现 DE063 字典搭配 DE066/067 等非相邻组合；
opcode 55 也已从误判的 CD 音轨空操作改为原 `5c35/0dbf:0314` 的 `10h..1fh`
反向灰阶显存转换。仍有若干完整菜单画面需要像素校准。

FIG 已接入真实 ORC 编队、随机遇敌、BA/AD/FMAN、原版随机序列、回合先制、物理攻击、
攻/辅能力、战斗物品、五计数器组合技、双效果、敌方 AI、逃跑、收服、奖励捕获及捕获兽
召唤/友军 AI。效果分派器 0x32..0x65 的 SP/ST 加载、帧复位、Mode-X 坐标、累积移动
路径和同页多层合成均已逐入口转写，SP/VOC 映射及物攻、死亡、逃跑的
K1/SP106/SP015/SV3 语音也走统一后端。四向主命令、攻击/自动、等待/逃跑/收服三级层级、
50 格物品/能力滑动列表、单敌人免选择、自动战斗跨回合与按键中断均已按 FIG 路径重写；
普通逃跑还使用原 pose `0->5`、成功后 SV3，以及失败时 `DATA:2ca5` 的固定四列红字框，
不再误套普通奇术 pose 4 或静默跳过失败反馈。
`MENU.RSK` 的原版框、滚动条、消息框、字图、物品预览和角色指示器已用于合成。
能力列表现按 `41a1` 在每行右侧绘制真实数值消耗；五行元素类能力则依
`10/08/04/02/01` 位绘制原 Big5 元素字。物品卡也从 `FIG.EXE DATA:2998`
加载 42 组原版类别名。能力固有禁用和物品预约分别使用原调色板索引 `6b`，
不再把资源不足或不可战斗使用的项目错误染色。选择不可用项目时也不再静默无响应：
`FIG.EXE DATA:2cb7/2cd1/2cf5/2d05/2de7/2e65` 的六条 Big5 提示现由
`3de8/3e19` 的原版 7×4 底框模态显示，关闭按键会被提示框消费。
角色增益效果 `63/66/67/68/69` 结束时还会按 `57d6/2338` 在对应底部角色卡上方显示
原 `DATA:2d8d..2db5` 四字状态卡，并保留 18 个 70 Hz tick 的停留时间；处理器就让
该卡留在屏幕上返回，不再伪造清洁页或三 tick 尾帧。选择器 `62h` 的主动法盾则在
四张 SP338 后保留末帧四 tick；被敌术消耗法盾时同样播四帧，但按 `2513` 立即返回。
效果 `61` 的双方驱散反馈也已保留：敌方清除队员 `+313c..+3164` 时依次显示
`DATA:2d37..2d83`，队员清除怪物护魔/力量/闪躲时按 `2375/55e4` 在怪物中心旁显示
三张原版小卡；每卡 18 tick，最后一张会像原版一样留屏而不追加清洁页。`+315c` 护魔
现优先于 `+3164` 法盾拦截敌方术法：前者不消耗并显示
`DATA:2d7b`，后者消耗一层并追加 selector 62/SP338 四帧；普通抗性免疫分别复现
怪物侧 `DATA:2e9b`“失效”卡和队员侧红色零值十帧上浮。回合末/回合初的九种队员
增益/异常恢复与三种怪物增益到期，也会按 `0af5..0da7` 原顺序逐张提示；怪物侧某一
计时器本回合刚归零时会立即显示并短路，低优先级计时器该回合不再递减。
怪物的五种持续异常也不再只有规则计时：`2deb/2eb3` 的 MENU 177 底卡及
`A1/A4/9E/A5/9F` 图标会在怪物右上按当前有效槽紧密纵排；`5a91` 设置的实际随机
时长随事件进入呈现状态，`0af5` 将计数减到零时则静默移除相应图标。
敌方行动造成队员死亡时，`2293` 还会把暴击倒数强制为 1，并只抽一个队员显示
`DATA:2d73`“可恶！”反应卡；抽到死亡/禁行动槽时不重抽的原版行为也已保留。该卡
自身五 tick 后仍进入 `22e0` 的共享五 tick，实际留屏十 tick，再清洁并停三 tick。
双方物攻现还会按 `2b08/296a/130d` 显示原“攻击/失败/闪躲”小卡和零伤害上浮；
队员 identity 语音在第一张姿态已经提交、三 tick 等待尚未开始时播放，暴击音仍位于
第三张姿态之后，非闪躲失败音位于武器页之后。怪物闪躲卡从 `130d` 尾跳返回，卡后不会
伪造一张清洁页；命中/零伤害的十帧数字则把末帧再保留五 tick，提交 HP 后立即重画。
怪物命中还恢复 `2939` 的整屏震动：把清洁页上移三行后与原页从偏移页开始交替八个
一 tick 帧，结束在原页，再进入附加状态和 `2ac7` 数字，而不是从动作卡直接跳到伤害。
怪物随机逃跑则区分成功和失败，
显示 `DATA:2d69/2ca5` 后才播放 SV3 或继续战斗。
所有正常敌方行动（命中、失败、闪躲、术法、媒介召消）现在也统一回到 `22e0`：把
处理器末页保留五 tick、重画清洁战场、再停三 tick；多目标事件只在最后目标执行一次，
死亡反应事件则接管这条尾部，不会每个事件各清一次屏。
敌方能力与自疗也会先按 `262f` 在怪物旁显示原六字能力名卡，并遵守六个静音 ID、
`56h -> SP072`、低效果号 `-> SP001` 的敌方专用语音映射及七 tick 停留。
怪物 generic 分支现显式区别于 special/自疗：事件保留 `23b1/2464` 来源，名称卡后
按 `25ee/6f32` 以抗性低三位选择 `5c/81/aa/7c/8d` 色号，整屏纯色与清洁战场从纯色
开始交替八个一 tick 帧；不再错误套用玩家 DS:2bbd 的 SP/ST 特效时间线。
多目标能力现只在首个目标播放一次施术姿态、语音和 SP/ST 时间线；其余目标保留各自
结算/护盾/死亡反馈，避免原先按事件重复整套演出的错误。
敌方全体术法还保留 `2485..24ea` 的槽循环：能力名卡后先无等待地提交清洁战场/全队
底卡，特效只播一次；随后每个队伍槽（包括死亡而没有结算事件的槽）都让当前末帧停
三 tick。`2ac7` 的第十张队员数字页在这三 tick 中原样留屏，不会被提前换成清洁页。
玩家奇术/道具的通用 `4338` 段现严格先画 FMAN pose `0 -> 4`：第一张停三 tick，
第二张翻页、备份到 A800 后同样停三 tick；随后 `436a` 才播放效果语音并进入 DS:2bbd 动画，
不再把前两张 SP/ST 特效错误叠在角色姿态下面。物攻 `13e8` 三姿态的停留也改为
`3/1/1` tick。组合效果 `6b` 的两个嵌套 selector 共用一次角色姿态，但各自保留一套
语音/特效；同 selector 的多目标仍只播一次。缺少媒介时同样先完成这组通用姿态，
再进入 `58fa` 的 SP002 提示。
捕获兽行动也已恢复 `10fc` 的固定双槽动作卡：第一/第二槽分别位于 Mode-X 列
`2/16h`、`y=15`，物攻、奇术、离场使用原 `DATA:2c9d/2e83/2e7b`，离场卡保留
色号 `6b` 和九 tick 停留。`2f1f/2f4c` 的两个常驻姓名卡也已恢复到 Mode-X
`x=0/14h,y=0`；召唤安装按 `2f84` 播放 SP061 并停留十 tick，不再误把捕获兽
ITEM 编号当作 ST 动画号。两槽已满时仍可选择捕获兽物品，`111b3/5d24` 会显示原
`DATA:2e8b` 双槽替换面板，把选中旧友军写回新物品所在背包格，再原位装入新友军。
炼妖壶命令则按 `0ded..0eac` 先播 SP016，在施术者底卡
显示 `DATA:2e5d`，失败时于右下叠加 `DATA:2c95`，成功时移除怪物并播 SP015；已有
召唤兽占用 `+3e4` 时也会像原版一样强制失败。
三个常驻媒介也已从 `23b1/2731/58fa/5b41` 接回统一战斗状态：怪物 generic 能力按
低字节标志召唤 MENU `AEh/AFh/B0h` 并退还预扣 AP，special `35h/43h/53h`
可将其移除；玩家、道具、组合技嵌套效果和捕获兽在缺少所需媒介时仍支付资源/消耗物品，
显示原 `DATA:2d1f` 面板并播放 SP002。SP049 飞行、SP061 移除和每帧常驻重画都只
消费 `BattleSession` 的平台无关事件，方便同一规则移植到其他图形后端。
胜负结算不再直接静默返回 RPG：胜利时播放 WI01、显示 `04bf` 的 10×3 金钱/人均经验
面板，可选 ORC 捕获奖励会叠加 `0553` 提示；失败时播放 DEAD 并显示 `0643` 红字框。
经验达到门槛后会在恢复临时战斗属性之后逐级进入 `07d2` 的 14×10 成长面板，显示角色
姓名、八项旧/新数值、所得奇术并播放 WI02，多级成长逐次确认。
type-10 道具的能力点成本、效果 47 特殊逃跑，以及效果 6b 的双效果道具/能力共用分派
也已按 `1138/57f2` 重写。效果 47 不再误播普通逃跑的 `SV3.VOC`：`4ef3` 会在
通用施术姿态后只播放 `SP071.VOC`、等待五 tick，再直接退栈至战斗退出或下一回合，
固定战也不会显示普通逃跑失败框。辅助效果 `01..30` 的事件还携带 `58a9` 提交后的
完整角色支持状态，使复活/解异常、第二资源与能力槽恢复、上限和永久属性 `+3` 都能在
下一张底卡一次性同步，不再只更新 HP 而留下旧资源条或死亡图标。呈现也不再误用
`2ac7` 恢复数字：单目标严格是 `5841` 的旧卡三 tick、提交、新卡九 tick；全体恢复按
`452a/45c7` 对整组事件原子执行旧卡九 tick/新卡九 tick；永久 `+3` 只预览三 tick 后
写入而不调用 `58a9`，选择器 `28/29h` 的两个 RET 则不伪造任何目标页。
选择器 `00h` 同样在公共姿态和 SP001 后直接返回，不再追加移植版曾虚构的清洁页/三 tick。
原版不存在的顶部敌我横向血条和目标描边已经移除，改由
`2bb5/2bd9/137a` 的 MENU 角色卡、三资源槽、死亡/低血量/状态图标和动作帧 180；
其中怪物的 `EFh` 遮罩、死亡 MENU 153 的零色遮罩会经 `798b` 的 40/64 调色板暗化表
混合下层画面，低血量 MENU 154 则使用 `7900/7960` 建立的 35/64 源色混合表，不再按
普通透明精灵覆盖；
命令选中按 `314d/771b/77ef` 从 BA 调色板生成原版四张最近色转换表并映射完整
64×32 选项块，不再绘制伪造边框。伤害/恢复结算直接使用 MENU 121/111 两组数字，
并分别按怪物 `144e` 与队员 `2ac7` 的坐标完整上浮十帧。怪物自己的行动不再错误切到
frame 1：物攻武器合成帧或首个 `144e` 数字帧才一次性消费受击目标 `+3114`；持续
异常即使随机伤害为零也保留零值数字和受击帧。剧情目录 30h 的六帧开场和目录 42h
的 CD523 受击替换亦已按 `32aa..3386/2e30` 转写。`14b1/152a/155c` 武器段也会在
三张 FMAN 姿态之后逐手呈现：左手水平镜像、坐标固定于目标中心 `-4` Mode-X 列/
`-50` 行，`+2c==1` 只播右手，而双空手仍加载原版 `SW000.RSK`。
每个武器页与随后清洁受击页现在还分别走 `3c44` 的四段自上而下揭示：每段复制
80 Mode-X 字节×50 行并等待一个 70 Hz tick，不再以一张静止画面等待三 tick 近似。
怪物周期伤害 `0b75..0c03` 也已去掉这一近似：清洁预备页不等待，十张数字之后提交
HP/清洁页并精确保留四 tick，而不是前后三 tick。
开场文字/YN-NY 问答已改用 `3de8/5c98` 的 MENU
7×4 原版框、精确文字原点、帧 29/8 选项和同一调色板选中算法。怪物/队员目标页现已
保留攻击、能力或物品来源页及原选择光标，再按 `178c/2d75/19a8` 叠加名称框或
MENU 143，取消时也精确返回原项；剩余工作主要是完整剧情战与全流程画面的像素级差分。

DEMO 已按原程序重建 960 帧主时间线和完整 SWORD8 尾声，并有无输入全程集成测试。
43 首 RIX 已转换为原驱动的精确 YM3812 寄存器序列，使用官方 `ymfm` YM3812 核合成，
并以 DOSBox-X 捕获做寄存器差分；67 个 Creative Voice type-1 文件均严格解码并可播放。
`SaveSlot` 已把 1..5 槽的 `SAVE.DAn`/`MAPZ.DAn` 成对载入和原位持久化；存档道具现以
`RPG.EXE` 内嵌 Big5 提示、`MENU.RSK` 通用面板/光标/确认框和原版非对称按键状态机显示，
确认框与出售的是/否卡还会经 `21de/772d` 把未选项映射到调色板表 3，并在写盘前执行
原版的道具消耗顺序。场景道具的四向菱形角色目标卡也恢复同一套选择调色：当前角色
保持原色，其余三卡变暗；`24b8` 的死亡、低血量和十一种状态覆盖同样完整呈现。读取
`RPG.EXE DATA:3ace` 34 组地名的十行传送面板/滚动条也已替换旧占位框；背包、装备等
菜单仍需继续像素校准。背包的 50 槽主列表现也使用 `2ae4/298d` 的八行 MENU 面板、
168×22 选择帧和比例滑块，并从 ITEM `+02/+1d/+1f` 加载真实 CD/AD 物品预览、
`DATA:299a` 类型字图和原 MENU 数字；`298d` 的 PgUp/PgDn/Home/End 也已进入平台无关
输入层，翻页时只移动首个可见项并保留光标所在行，不再用左右箭头伪造整页跳转。
`2907` 滚动条的 MENU 94/95、97/98 端点按键闪帧也随滚动方向显示，frame 99 滑块从
面板 `top+20` 按商余分配定位，不再错误压在顶部箭头上。SDL 键盘直接映射这四键，
手柄则用左右肩键和 X/Y，其他平台可以复用同一语义动作。
当前代码刻意不把
反编译器生成的伪 C 当作可维护源码。商店购买列表也已换成 `5766` 的五行 MENU 面板、
原始内嵌提示、类型卡和同比例滚动条；购买的金钱/容量/20 件上限反馈及购买确认、出售的
三季度价格提示也都恢复 `DATA:3c1e/3c44/3c54/3c6c/3c80` 与共用 `555a` 是/否页。
装备页使用 `46b1` 的 8×11 全屏面板、ME02 原画、
`DATA:388e/3a60` 槽位/属性字图、原帧光标、角色资源卡和四项真实属性数值；角色禁用、
类别错槽与双手冲突会分别显示原 `DATA:369a/3728/370e` 底部消息，不再无反馈地返回。

移植版始终只生成一个游戏可执行文件；原版 EXE 与新模块的合并关系见
[`architecture.md`](architecture.md)。

## 构建

```sh
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

查看原版可执行文件和资源清单：

```sh
./build/src/swd2_rewrite --game game --inspect
./build/src/swd2_rewrite --game game --verify-resources
./build/src/swd2_rewrite --game game --verify-maps
./build/src/swd2_rewrite --game game --verify-battles
./build/src/swd2_rewrite --extract game/MEO.RSK MEO.decoded
./build/src/swd2_rewrite --render game/MEO.RSK 0 MEO-background.ppm
./build/src/swd2_rewrite --game game --render-meo MEO-screen.ppm
./build/src/swd2_rewrite --render-map game/T2/TW-2A TW-2A.ppm
./build/src/swd2_rewrite --render-planar game/DE/DE001 17 DE001-17.ppm
./build/src/swd2_rewrite --game game \
  --save-dir portable-saves --slot 2 \
  --run-script CONFIRM,CONFIRM,CONFIRM,RIGHT,DOWN,QUIT
```

模拟并验证启动器协议。列表中的每一项是相应子模块返回的共享标记：

```sh
./build/src/swd2_rewrite --trace MT,ED,--,IF,OC,--
```

详见 [`reverse-engineering.md`](reverse-engineering.md)。
