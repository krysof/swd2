# 《轩辕剑2》C++ 重写工程

原始游戏的 2,315 个文件均已移到仓库根目录的 `game/`，本目录只放逆向记录和新写的代码。

## 当前可运行部分

- DOS MZ 可执行文件解析器（头、重定位、入口、附加数据）；
- `RSK/RS1..RS4/RAP/RRO` 的原版 64 KiB LZ/Huffman 解码器；
- 原始 `SWD2.EXE` 启动/调度状态机的等价 C++ 实现；
- DOS 资源路径规范化与资源目录统计；
- MAPA/MAPZ 世界数据库、MAP0 的 481 条地图入口/特殊事件、SA 地图实体精灵、DE 平面演出、
  CHNA 事件与 Big5 对话；
- SDL 单窗口前端，以及合入同一进程的 MEO、RPG、FIG、DEMO 模块；
- 自动测试直接使用 `game/` 原始数据校验启动、地图、事件、对话和模块切换。

这仍不是完整游戏成品，但四个主程序已没有退出占位。MEO 流程可运行；RPG 的全部
62 个事件调度号、地图切换、三格实体碰撞、拐角滑行、自动实体事件和原版代码窗口驱动的
实体游走已进入可移植 VM。最多四名地图队员现按每人三个延迟槽跟随转角，实体与队员按
`dd6/4f1c` 的 31 层基线顺序交错绘制，脚步动画和视口滚动也保留原队列状态。商店、背包
使用、装备交换、精确出售及存档数据提交均可运行；野外每十步毒伤、昏迷警告/闪页和
`1fa8` 四次代码窗口抽样后的随机 FIG 遇敌也已接入同一进程；行为 6 实体在普通碰撞时
按 `5298` 静默隐藏，而显式确认仍可越过该分支执行事件；
opcode 13 已按 `54b8/555a` 恢复原是/否条件分支及选择后的来源页回显，而不是误开背包；
opcode 14 金钱条、DE 过场、RI 事件音乐、VOC、调色板淡入淡出、逐帧延时、mode-X
地址抖动和坐标文字也已按机器码路径重写。DE 过场会分别跟踪 opcode 29/43 选择的
图块字典与 opcode 35 的 RAP 布局，因而可复现 DE063 字典搭配 DE066/067 等非相邻组合；
opcode 55 也已从误判的 CD 音轨空操作改为原 `5c35/0dbf:0314` 的 `10h..1fh`
反向灰阶显存转换。仍有若干完整菜单画面需要像素校准。

FIG 已接入真实 ORC 编队、随机遇敌、BA/AD/FMAN、原版随机序列、回合先制、物理攻击、
攻/辅能力、战斗物品、五计数器组合技、双效果、敌方 AI、逃跑、收服、奖励捕获及捕获兽
召唤/友军 AI。效果分派器 0x32..0x65 的 SP/ST 加载、帧复位、Mode-X 坐标、累积移动
路径和同页多层合成均已逐入口转写，SP/VOC 映射及物攻、死亡、逃跑的
K1/SP106/SP015/SV3 语音也走统一后端；捕获兽 SP106 在攻防/闪躲判定前播放，
所以未命中仍保留原声音。四向主命令、攻击/自动、等待/逃跑/收服三级层级、
50 格物品/能力滑动列表、单敌人免选择、自动战斗跨回合与按键中断均已按 FIG 路径重写；
普通逃跑还使用原 pose `0->5`、成功后 SV3，以及失败时 `DATA:2ca5` 的固定四列红字框，
不再误套普通奇术 pose 4 或静默跳过失败反馈。
`MENU.RSK` 的原版框、滚动条、消息框、字图、物品预览和角色指示器已用于合成。
能力列表现按 `41a1` 在每行右侧绘制真实数值消耗；五行元素类能力则依
`10/08/04/02/01` 位绘制原 Big5 元素字。物品卡也从 `FIG.EXE DATA:2998`
加载 42 组原版类别名。能力固有禁用和物品预约分别使用原调色板索引 `6b`，
不再把资源不足或不可战斗使用的项目错误染色。选择不可用项目时也不再静默无响应：
`FIG.EXE DATA:2cb7/2cd1/2cf5/2d05/2de7/2e65` 的六条 Big5 提示现由
`3de8/3e19` 的原版 7×4 底框模态显示，关闭按键会被提示框消费。
角色增益效果 `63/66/67/68/69` 结束时还会按 `57d6/2338` 在对应底部角色卡上方显示
原 `DATA:2d8d..2db5` 四字状态卡，并保留 18 个 70 Hz tick 的停留时间；处理器就让
该卡留在屏幕上返回，不再伪造清洁页或三 tick 尾帧。选择器 `62h` 的主动法盾先完成
五级变暗并换成普通目标卡，再从 `party*18+10` 精确播放四张 SP338、保留末帧四 tick；
资源扣除与五级恢复均在处理器返回后发生，随后仍有 `0d98` 裸战场边界。被敌术消耗
法盾时同样播四帧，但按 `2513` 立即返回。
效果 `61` 的双方驱散反馈也已保留：敌方清除队员 `+313c..+3164` 时依次显示
`DATA:2d37..2d83`，队员清除怪物护魔/力量/闪躲时按 `2375/55e4` 在怪物中心旁显示
三张原版小卡；玩家路径现也经过 `43ce` 五级变暗，在 SP336 五帧与 SP337 三帧之间
提交原版暗色清洁页，每卡 18 tick 后延迟扣费，并让最后一张卡穿过 `4417` 五级恢复，
最后才进入 `0d98` 裸战场。对应 pose、八张 SP 页、中间清洁页、驱散卡和尾页均与新鲜
原版捕获逐 RGB 像素一致。`+315c` 护魔
现优先于 `+3164` 法盾拦截敌方术法：前者不消耗并显示
`DATA:2d7b`，后者消耗一层并追加 selector 62/SP338 四帧；普通抗性免疫分别复现
怪物侧 `DATA:2e9b`“失效”卡和队员侧红色零值十帧上浮。回合末/回合初的九种队员
增益/异常恢复与三种怪物增益到期，也会按 `0af5..0da7` 原顺序逐张提示；怪物侧某一
计时器本回合刚归零时会立即显示并短路，低优先级计时器该回合不再递减。
怪物的五种持续异常也不再只有规则计时：`2deb/2eb3` 的 MENU 177 底卡及
`A1/A4/9E/A5/9F` 图标会在怪物右上按当前有效槽紧密纵排；`5a91` 设置的实际随机
时长随事件进入呈现状态，`0af5` 将计数减到零时则静默移除相应图标。
敌方行动造成队员死亡时，`2293` 还会把暴击倒数强制为 1，并只抽一个队员显示
`DATA:2d73`“可恶！”反应卡；抽到死亡/禁行动槽时不重抽的原版行为也已保留。该卡
自身五 tick 后仍进入 `22e0` 的共享五 tick，实际留屏十 tick，再清洁并停三 tick。
双方物攻现还会按 `2b08/296a/130d` 显示原“攻击/失败/闪躲”小卡和零伤害上浮；
队员 identity 语音在第一张姿态已经提交、三 tick 等待尚未开始时播放，暴击音仍位于
第三张姿态之后，非闪躲失败音位于武器页之后。怪物闪躲卡从 `130d` 尾跳返回，卡后不会
伪造一张清洁页；命中/零伤害的十帧数字保留最终 pose 2/3 而不换回普通头像，并把末帧
再保留五 tick。SW 页先于 `137a` 的动作卡合成，避免伸入 y=150 的武器像素反盖人物卡；
提交 HP 后，战斗继续才恢复普通队员卡，回合边界则直接使用无队员卡的裸 `2db8` 页面。
怪物命中还恢复 `2939` 的整屏震动：把清洁页上移三行后与原页从偏移页开始交替八个
一 tick 帧；偏移页未覆盖的底部三行保持黑色，结束在原页，再进入附加状态和 `2ac7`
数字，而不是从动作卡直接跳到伤害。
怪物随机逃跑则区分成功和失败，
显示 `DATA:2d69/2ca5` 后才播放 SV3 或继续战斗。
所有正常敌方行动（命中、失败、闪躲、术法、媒介召消）现在也统一回到 `22e0`：把
处理器末页保留五 tick、重画清洁战场、再停三 tick；多目标事件只在最后目标执行一次，
死亡反应事件则接管这条尾部，不会每个事件各清一次屏。
敌方能力与自疗也会先按 `262f` 在怪物旁显示原六字能力名卡，并遵守六个静音 ID、
`56h -> SP072`、低效果号 `-> SP001` 的敌方专用语音映射及七 tick 停留。
怪物 generic 分支现显式区别于 special/自疗：事件保留 `23b1/2464` 来源，名称卡后
按 `25ee/6f32` 以抗性低三位选择 `5c/81/aa/7c/8d` 色号，整屏纯色与清洁战场从纯色
开始交替八个一 tick 帧；不再错误套用玩家 DS:2bbd 的 SP/ST 特效时间线。
已发布怪物使用的 `49h`“炒飯十八手”和 `4ch`“獅子吼”在 `26af` 中
没有分派：`283f..2844` 会直接跳到 `2938` 的 `RET`。它们仍消耗已扣除的能力点和本回合，
但不显示 `262f` 名称卡、不播语音、不调用玩家同号 selector 特效；只保留 `20e7/22e0`
两张无人物卡的裸战场页。目录962的原版录像已逐页固定这一静默返回路径。
`26af` 成功对队员施加状态的另一分支也已逐页还原：裸准备页后先提交被选队员卡，
再叠加能力名；七 tick 后在保留名称卡的页面上提交状态图标，最后才进入五/三 tick
的 `22e0` 裸页尾部。目录962、怪物456的“纏靈咒”固定轨迹已与原版七个 RGB 页零差异。
同一效果在队员第三抗性字节为 1 时会从 `26f1` 直接跳到 `2721`：仍提交裸准备页和
被选队员卡，但完全跳过名称卡、语音、状态写入及七 tick 等待，再走五/三 tick 公共尾部。
该抗性轨迹的五张稳定页也已与原版完整 RGB 零差异。
怪物自身的 `67h..69h` 临时增强也不复用队员目标页：`20e7` 裸页上直接显示 `262f`
名称卡，状态只写怪物内部计时器/属性，公共尾部仍回到裸页。目录1246的 effect `67h`
轨迹已锁定五张原版 RGB 页及名称七 tick、尾部五/三 tick 时序。
多目标能力现只在首个目标播放一次施术姿态、语音和 SP/ST 时间线；其余目标保留各自
结算/护盾/死亡反馈，避免原先按事件重复整套演出的错误。
敌方全体术法还保留 `2485..24ea` 的槽循环：能力名卡后先无等待地提交清洁战场/全队
底卡，特效只播一次；随后每个队伍槽（包括死亡而没有结算事件的槽）都让当前末帧停
三 tick。`2ac7` 的第十张队员数字页在这三 tick 中原样留屏，不会被提前换成清洁页。
玩家奇术/道具的通用 `4338` 段现严格先画 FMAN pose `0 -> 4`：第一张停三 tick，
第二张翻页、备份到 A800 后同样停三 tick；随后 `436a` 才播放效果语音并进入 DS:2bbd 动画，
不再把前两张 SP/ST 特效错误叠在角色姿态下面。物攻 `13e8` 三姿态的停留也改为
`3/1/1` tick。组合效果 `6b` 的两个嵌套 selector 共用一次角色姿态，但各自保留一套
语音/特效；同 selector 的多目标仍只播一次。缺少媒介时同样先完成这组通用姿态，
再进入 `58fa` 的 SP002 提示。
捕获兽行动也已恢复 `10fc` 的固定双槽动作卡：第一/第二槽分别位于 Mode-X 列
`2/16h`、`y=15`，物攻、奇术、离场使用原 `DATA:2c9d/2e83/2e7b`，离场卡保留
色号 `6b` 和九 tick 停留。`2f1f/2f4c` 的两个常驻姓名卡也已恢复到 Mode-X
`x=0/14h,y=0`；召唤安装按 `2f84` 播放 SP061 并停留十 tick，不再误把捕获兽
ITEM 编号当作 ST 动画号；安装页十 tick 仍显示付费前蓝条，`5de4` 的等级两倍费用
从下一张战斗页开始才反映到该条。
两槽已满时仍可选择捕获兽物品，`111b3/5d24` 会显示原
`DATA:2e8b` 双槽替换面板，把选中旧友军写回新物品所在背包格，再原位装入新友军。
战斗物品在一回合解析完成后按原版稳定压紧；替换页保留解析期的角色 pose 0 动作卡，
不再误用普通命令头像和 MENU 178 剑形标记，并已有原版整页 RGB 零差异回归。
捕获兽普通物攻结果和奇术的内部返回还统一经过 `1039/0fb9` 的清洁重画与五 tick 尾部；
缺媒介、状态卡、直接 RET 和多目标事件都不会漏掉或按目标重复这段调用者时序。闪躲则
按 `0ffa -> 2a28` 留下怪物中心的红色“闪躲”卡八 tick，明确绕过该清洁尾部。
捕获兽攻值未超过防御时仍按 `100b/144e` 上浮红色零值十帧，再清洁并停五 tick，
不会静默跳过这次失败结算。目录 `64h` 的捕获物品 420 两回合基准进一步锁定了
裸 `2db8 -> 10fc -> 144e` 的准备页、无底部队员卡动作页、十张零值页、既有
`E0h..E4h` 色表轮转以及 `1039` 裸清洁尾部；除采样跨过的第一张数字页外，登记页均与
原版完整 320×200 RGB 一致。
随机离场则独立按 `0eef` 在九 tick 红色卡后播放 SV3、压紧槽、清洁并停四 tick，
不会再多画一张仍含旧捕获兽的三 tick 页面。
炼妖壶命令则按 `0ded..0eac` 先播 SP016，在施术者底卡
显示 `DATA:2e5d`，失败时于右下叠加 `DATA:2c95`，成功时移除怪物并播 SP015；已有
召唤兽占用 `+3e4` 时也会像原版一样强制失败。
三个常驻媒介也已从 `23b1/2731/58fa/5b41` 接回统一战斗状态：怪物 generic 能力按
低字节标志召唤 MENU `AEh/AFh/B0h` 并退还预扣 AP，special `35h/43h/53h`
可将其移除；玩家、道具、组合技嵌套效果和捕获兽在缺少所需媒介时仍支付资源/消耗物品，
显示原 `DATA:2d1f` 面板并播放 SP002。SP049 飞行、SP061 移除和每帧常驻重画都只
消费 `BattleSession` 的平台无关事件，方便同一规则移植到其他图形后端。
玩家 selector `31h/3bh/3ch` 本身则是三种媒介的直接安装处理器，不属于上述缺媒介
前置判断；它们从角色动作锚点播放 SP049 飞行并写回常驻槽。物品 195 的 `31h+3ch`
组合现按一次公共 pose/变暗/付费包络连续安装 AE/B0，七张稳定页与原版 RGB 完全一致。
捕获兽还保留 `1048` 的前置召唤差异：其能力记录 `80h/40h/20h` 请求的媒介不存在时，
从槽位 `x+0fh,y=0fh` 播放 SP049 飞行并结束本次行动，但不会像敌方 `23b1` 一样退款，
也不会误显示玩家 `58fa` 的缺媒介框；后续施放才进入普通玩家侧 selector。
胜负结算不再直接静默返回 RPG：胜利时播放 WI01、显示 `04bf` 的 10×3 金钱/人均经验
面板，可选 ORC 捕获奖励会叠加 `0553` 提示；失败时播放 DEAD 并显示 `0643` 红字框。
经验达到门槛后会在恢复临时战斗属性之后逐级进入 `07d2` 的 14×10 成长面板，显示角色
姓名、八项旧/新数值、所得奇术并播放 WI02，多级成长逐次确认。
type-10 道具的能力点成本、效果 47 特殊逃跑，以及效果 6b 的双效果道具/能力共用分派
也已按 `1138/57f2` 重写。效果 47 不再误播普通逃跑的 `SV3.VOC`：`4ef3` 会在
通用施术姿态后只播放 `SP071.VOC`、等待五 tick，再直接退栈至战斗退出或下一回合，
固定战也不会显示普通逃跑失败框。辅助效果 `01..30` 的事件还携带 `58a9` 提交后的
完整角色支持状态；直接战术道具也不再绕过 `61..69h` 状态分派，原版物品
186/204/211/226 的护法、法盾、驱散和速度效果会使用 ITEM 自身编号呈现并正确付费/消耗；
其中物品 226 还必须服从内嵌能力 86 的 target-flags 媒介 AG，缺失时按原版在单张
pose 0 后变暗、显示 `58fa` 卡、付费并消耗，而不是错误施加速度。
`1138` 的直接道具公共入口只有一张 pose 0 和三 tick，不会误套能力 `4338` 的第二张
pose 4；物品 211 的 pose 0、暗页、八张驱散 SP、结果卡和裸尾均已与原版逐页匹配。
非消耗物品 186 进一步锁定 `57d6`：暗色状态卡先覆盖普通头像，`4417` 恢复卡才保留
直接道具的 pose 0；两类卡、恢复调色板与裸尾均与原版稳定页零 RGB 差异。
`6b` 也不再只接受 type-10/已学奇术映射：所有直接道具都从自身 ITEM `+9/+0a` 派发，
包括编号低于 `8ch` 的 16-bit 回绕项和物品 248 的 `08+0f` 队员恢复/净化组合。
ITEM 目标位现在只决定命令收集页，不再错误决定 selector 类别；无目标的环境道具
`31/3b..3f` 及其他 `>30h` 记录仍进入 FIG 效果表。124 件已发布、非捕获兽且战斗位
有效的道具现逐件执行回归，均不会落入 invalid command。
ORC 四张成长表与初始 SAVE 合并得到的 75 个可达角色奇术中，四个有明确战斗禁用位，
70 个逐项通过真实回合分派；剩余能力 41 是原版 `0500h/11h` 的空目标指针 8 位 DIV
崩溃，现代命令页明确禁用而不再虚构自疗。ITEM 怪物记录引用的 19 个 special 能力 ID
则逐项验证不会返回 unsupported。低编号但仅供敌方/捕获兽使用的记录不会被臆造成可学奇术。
捕获兽的 generic/A/B 三槽合并后共有 68 个能力 ID；原版 `1048` 将它们送回玩家效果表，
而不是敌方 `26af`，现也已按这一真实入口逐项验证支持、战术和伤害/异常三类分派。
这些快照使复活/解异常、第二资源与能力槽恢复、上限和永久属性 `+3` 都能在
下一张底卡一次性同步，不再只更新 HP 而留下旧资源条或死亡图标。呈现也不再误用
`2ac7` 恢复数字：单目标严格是 `5841` 的旧卡三 tick、提交、新卡九 tick；全体恢复按
`452a/45c7` 对整组事件原子执行旧卡九 tick/新卡九 tick；永久 `+3` 只预览三 tick 后
写入而不调用 `58a9`，选择器 `28/29h` 的两个 RET 则不伪造任何目标页。
能力 68/effect `02h` 还用未封顶的 100/1000 双资源卡固定 `586f` 的
`45% + 施术者智慧/4`，九个动作/结果检查页和原版完整 RGB 相同。
能力 44/effect `0ah` 则只恢复第二资源，固定同一个 `45% + 智慧/4`
结果，同时验证 class-five 五行素材直到公共费用尾部才各扣一份。
能力 103/effect `0bh` 将同一单槽提高到 `70% + 智慧/4`，并锁定
class-four AP 的 50 点延迟扣费页。
能力 112/effect `0dh` 还固定全体 `45%/45%` 双资源分支的旧卡、提交、
新卡和 class-one 100 点延迟扣费边界。
能力 113/effect `0eh` 对应的全体 `70%/100%` 分支也用独立结果摘要和
六张原版 RGB 检查页锁定。
能力 47/effect `1ah` 还固定能力资源单槽的 `200 + 智慧/4` 固定值分支及
class-five 延迟素材支付。
能力 42/effect `1eh` 固定只清除 `0200h` 异常位、不改三资源数值的单目标
结果卡和同一 class-five 尾部。
另外锁定可达重复记录 45/101/108/109，覆盖 selector `01/03/02/19h` 在
class-five、class-four、class-one、class-two 四种实际支付来源；至此发行版 18 条
可达玩家支援能力中，17 条正常路径均有逐页原版 RGB 证据，能力 41 则由原版 DIV
故障捕获和安全禁用证据单独封闭。
可达能力 54/effect `32h` 在先安装 AE 媒介后锁定 SP043..050 上下双层四步五轮、
稳定双方伤害、恢复及完整后继怪物回合，共 62 张原版 RGB 检查页。
可达能力 60/effect `34h` 在先安装 AE 媒介后锁定 SP052/SP552 四帧五轮、完整伤害、
稳定恢复和保留 AE 的胜利奖励边界，共 43 张原版 RGB 检查页。
可达能力 59/effect `35h` 在先安装 AE 媒介后锁定 SP000..023 上下双层十二步两轮、
完整伤害、五级恢复和保留 AE 的胜利奖励边界，共 48 张原版 RGB 检查页。
可达能力 88/effect `36h` 在先安装 AE 媒介后锁定 SP054 四帧冰轮、九张稳定伤害、
恢复和保留 AE 的胜利奖励边界，共 25 张原版 RGB 检查页。
可达能力 73/effect `37h` 在先安装 AF 媒介后锁定 SP024..033 十张火龙页、完整伤害、
五级恢复和保留 AF 的胜利奖励边界，共 34 张原版 RGB 检查页。
可达能力 76/effect `38h` 的伏虎符锁定 SP034/SP035 各四帧和 SP036 一帧、完整双方
伤害、恢复及后继怪物回合，共 51 张原版 RGB 检查页。
可达能力 78/effect `39h` 的致死降龍符锁定 SP037 六帧、SP038 五帧、完整十张伤害、
末三级恢复和胜利奖励边界，共 34 张原版 RGB 检查页。
可达能力 77/effect `3ah` 的驚鬼符锁定 SP039/SP040 各两帧和 SP041 三帧、完整双方
伤害、恢复及后继怪物回合，共 49 张原版 RGB 检查页。
可达能力 57/effect `40h` 在先安装 AE 媒介后锁定 SP200..207 上下双层四步五轮、
完整伤害、五级恢复和保留 AE 的胜利奖励边界，共 44 张原版 RGB 检查页。
可达能力 70/effect `41h` 的火珠符锁定 SP208..219 上下双层六步两轮、完整玩家伤害、
恢复和完整后继怪物回合，共 55 张原版 RGB 检查页。
可达能力 74/effect `42h` 的三昧真火符锁定 SP220..225 十二张行进页、SP228..231
四张双层页、双方伤害、恢复和完整后继怪物回合，共 58 张原版 RGB 检查页。
可达能力 89/effect `44h` 的致死鬼哭符锁定 SP246 五级下落及末三帧、SP247 两帧、
九张稳定伤害、恢复尾部和胜利奖励边界，共 31 张原版 RGB 检查页。
可达能力 95/effect `46h` 的致死五破符锁定 SP258 十帧两轮、九张稳定伤害、末三级
恢复和胜利奖励边界，共 42 张原版 RGB 检查页。
可达能力 84/effect `48h` 在先安装 B0 媒介后锁定 SP259..266 八张风叶页、九张
稳定玩家伤害、四级稳定恢复和完整后继怪物回合，共 50 张原版 RGB 检查页。
可达能力 5/effect `49h` 的炒飯十八手现锁定 SP267 四帧、SP268/SP269 各三帧、
白闪与双方十张伤害、稳定恢复和完整后继怪物回合，共 52 张原版 RGB 检查页。
可达能力 11/effect `4ah` 的公共双姿态、四级暗化、SP074 两轮八帧、白闪与十页
伤害、稳定调色板恢复、费用边界及后继怪物动作也已有 38 张原版 RGB 检查页。
可达能力 10/effect `4ch` 随后锁定公共姿态、四级稳定暗化、SP076 收缩/点阵扩散
十帧、白闪、九张稳定伤害页、恢复尾部和完整后继怪物回合，共 51 张原版 RGB 页。
可达能力 13/effect `4dh` 的瘋狗咬貓拳锁定 SP283 三帧拳影两轮、十张双方伤害、
恢复尾部和完整后继怪物回合，共 48 张稳定原版 RGB 检查页。
可达能力 1/effect `4eh` 的致死迷蹤猴拳锁定 SP284 四帧拳影两轮、白闪、十张伤害、
五级恢复和胜利奖励边界，共 32 张稳定原版 RGB 检查页。
可达能力 15/effect `4fh` 的 SP079 四帧拳影两轮、稳定伤害/恢复及后继回合也已
固定为 49 张逐页原版 RGB 检查点。
可达能力 17/effect `50h` 的驚虎吞狗掌锁定 SP286 两帧拳影四轮、双方十张伤害、
恢复尾部和完整后继怪物回合，共 50 张稳定原版 RGB 检查页。
可达能力 16/effect `5ah` 的 SP090 星点到纵向光柱八帧、稳定伤害/恢复与后继回合
也已有 50 张按时间排序的原版 RGB 检查页。
可达能力 18/effect `51h` 的 SP081 五帧残影拳两轮、完整十张玩家伤害、恢复及后继
回合则固定为 52 张原版 RGB 检查页。
可达能力 19/effect `52h` 的致死路径已锁定 SP082 十帧拳/红球、十张伤害、敌人移除、
恢复和胜利奖励边界，共 33 张原版 RGB 检查页。
可达能力 21/effect `53h` 的致死跛象撲虎功锁定 SP289..294 六张光束页、九张稳定
伤害、恢复尾部和胜利奖励边界，共 27 张稳定原版 RGB 检查页。
可达能力 20/effect `4bh` 的致死 SP075 阴阳球到八柱光束、伤害、恢复和胜利边界
也已有 28 张原版 RGB 检查页。
可达能力 22/effect `54h` 的致死 SP084 竖线到红色裂球、完整十张伤害、恢复和胜利
边界固定为 31 张原版 RGB 检查页。
可达能力 25/effect `55h` 的致死狂龍跌象拳锁定 SP297..299 六张光束/爆裂页、九张
稳定伤害、恢复尾部和胜利奖励边界，共 27 张稳定原版 RGB 检查页。
可达能力 23/effect `5bh` 的致死 SP091 落雷到横向切线、十张伤害、恢复和胜利边界
固定为 28 张原版 RGB 检查页。
可达能力 24/effect `5ch` 的致死 SP092 金环到多柱光束、完整十张伤害、恢复和胜利
边界固定为 32 张原版 RGB 检查页。
可达能力 26/effect `56h` 的致死 SP086 火团拳影两轮、完整十张伤害、恢复和胜利
边界同样固定为 32 张原版 RGB 检查页。
可达能力 27/effect `57h` 的致死 SP087 火团/蓝白拳影两轮、完整伤害、恢复和胜利
边界同样已有 32 张原版 RGB 检查页。
可达能力 28/effect `58h` 的致死 SP088 扇形线束到宽光柱、完整伤害、恢复和胜利
边界固定为 30 张原版 RGB 检查页。
可达能力 29/effect `59h` 的致死 SP089 十二帧剑影、完整伤害、恢复和胜利边界
固定为 35 张原版 RGB 检查页。
可达能力 30/effect `5dh` 的致死 SP093 十三帧放射剑阵、完整伤害、五级恢复和胜利
边界固定为 38 张原版 RGB 检查页。
可达能力 6/effect `5eh` 的怪物抗性路径又锁定 SP332 八帧、失效卡、付费后恢复、
玩家裸边界和完整后继怪物回合，共 42 张原版 RGB 检查页。
可达能力 72/effect `5fh` 的成功定身路径也锁定 SP333 纸符五帧、SP334 燃烧定字
五帧、状态安装、恢复尾部和下一命令页，共 21 张稳定原版 RGB 检查页。
可达能力 93/effect `60h` 的成功五毒路径则锁定 SP335 十二帧、MENU 158 持久状态卡、
恢复尾部、两个裸回合边界和下一命令页，共 25 张稳定原版 RGB 检查页。
可达能力 56/effect `64h` 的成功霧縛路径又锁定 SP340/341/342 共二十帧、MENU 165
持久状态泡、恢复尾部和下一命令页，共 32 张稳定原版 RGB 检查页。
可达能力 58/effect `65h` 的成功封魔路径还锁定 SP345..408 三十二张上下双层合成页、
MENU 159 持久状态泡、恢复尾部和下一命令页，共 43 张稳定原版 RGB 检查页。
选择器 `00h` 同样在公共姿态和 SP001 后直接返回，不再追加移植版曾虚构的清洁页/三 tick。
原版不存在的顶部敌我横向血条和目标描边已经移除，改由
`2bb5/2bd9/137a` 的 MENU 角色卡、三资源槽、死亡/低血量/状态图标和动作帧 180；
其中怪物的 `EFh` 遮罩、死亡 MENU 153 的零色遮罩会经 `798b` 的 40/64 调色板暗化表
混合下层画面，低血量 MENU 154 则使用 `7900/7960` 建立的 35/64 源色混合表，不再按
普通透明精灵覆盖；
命令选中按 `314d/771b/77ef` 从 BA 调色板生成原版四张最近色转换表并映射完整
64×32 选项块，不再绘制伪造边框。伤害/恢复结算直接使用 MENU 121/111 两组数字，
并分别按怪物 `144e` 与队员 `2ac7` 的坐标完整上浮十帧。`144e` 先把无数字受击页保存到
`A800h`，十页数字都保留同一反应精灵，并按 canonical ability 的低三位选择火/水/中性
`E0h..E4h` 五色表、每两页循环一次。怪物自己的行动不再错误切到
frame 1：物攻武器合成帧或首个 `144e` 数字帧才一次性消费受击目标 `+3114`；持续
异常即使随机伤害为零也保留零值数字和受击帧。剧情目录 30h 的六帧开场和目录 42h
的 CD523 受击替换亦已按 `32aa..3386/2e30` 转写。`14b1/152a/155c` 武器段也会在
三张 FMAN 姿态之后逐手呈现：左手水平镜像、坐标固定于目标中心 `-4` Mode-X 列/
`-50` 行，`+2c==1` 只播右手，而双空手仍加载原版 `SW000.RSK`。
每个武器页与随后清洁受击页现在还分别走 `3c44` 的四段自上而下揭示：每段复制
80 Mode-X 字节×50 行并等待一个 70 Hz tick，不再以一张静止画面等待三 tick 近似。
目录 `188h` 的物攻姿态、SW122 三张稳定揭示页、全部十张 pose-2 数字、回合清洁和胜利页
共 18 张现代 RGB 页面已与新鲜原版捕获逐字节相同。
怪物周期伤害 `0b75..0c03` 也已去掉这一近似：清洁预备页不等待，十张数字之后提交
HP/清洁页并精确保留四 tick，而不是前后三 tick。预备、受击 scratch、全部零值数字和
尾页均保持裸 `2db8`（无底部队员卡），`5ed8` 轮转进入时已有的 `E0h..E4h` 而不另装
canonical 色表；固定随机游标的真实零值分支已有十一张稳定页面与原版 RGB 完全相同。
能力 52/effect `33h` 还把 `43ce/4417` 公共包络验证扩大到真实伤害处理器：完整 SP
动画和十帧 `144e` 数字均保留暗色目标锚定 pose 4，HP 提交后才由 `585e` 扣资源并
五步恢复。能力 100/effect `54h` 的 target mode 虽为零，仍隐式结算怪物槽零；动作/SP
横向锚点保留施术者而数字纵坐标取怪物中心。该分支从姿态、SP319、无数字受击页、十张
循环调色数字、恢复、无队员卡的 `2db8` 清洁页直到命令页共 22 张现代 RGB 页面均与新鲜
原版捕获逐字节相同。回合清洁页不再伪造普通队员头像；下一次命令合成才装回队员卡。
这些检查点仍不等于全部 FIG 路径完成。
开场文字/YN-NY 问答已改用 `3de8/5c98` 的 MENU
7×4 原版框、精确文字原点、帧 29/8 选项和同一调色板选中算法。怪物/队员目标页现已
保留攻击、能力或物品来源页及原选择光标，再按 `178c/2d75/19a8` 叠加名称框或
MENU 143，取消时也精确返回原项；剩余工作主要是完整剧情战与全流程画面的像素级差分。

DEMO 已按原程序重建 960 帧主时间线和完整 SWORD8 尾声，并有无输入全程集成测试。
43 首 RIX 已转换为原驱动的精确 YM3812 寄存器序列，使用官方 `ymfm` YM3812 核合成，
并以 DOSBox-X 捕获做寄存器差分；67 个 Creative Voice type-1 文件均严格解码并可播放。
`SaveSlot` 已把 1..5 槽的 `SAVE.DAn`/`MAPZ.DAn` 成对载入和原位持久化；两半先写临时
文件，再用 `SWD2PAIR1` 标记按 MAPZ→SAVE 提交，中断后下次打开会继续同一事务，未立标
记的残留临时文件则作为旧事务丢弃，不会再暴露一新一旧的槽位；存档道具现以
`RPG.EXE` 内嵌 Big5 提示、`MENU.RSK` 通用面板/光标/确认框和原版非对称按键状态机显示，
确认框与出售的是/否卡还会经 `21de/772d` 把未选项映射到调色板表 3，并在写盘前执行
原版的道具消耗顺序。场景道具的四向菱形角色目标卡也恢复同一套选择调色：当前角色
保持原色，其余三卡变暗；`24b8` 的死亡、低血量和十一种状态覆盖同样完整呈现。读取
`RPG.EXE DATA:3ace` 34 组地名的十行传送面板/滚动条也已替换旧占位框；背包、装备等
菜单仍需继续像素校准。前端退出检查点也使用 RPG 当前的完整 SAVE/MAPZ；新游戏在 OM
换成 `SAVE.DAQ/MAPZ.DAQ` 后不会把新 SAVE 错配到启动时选槽的旧 MAPZ。背包的 50 槽
主列表现也使用 `2ae4/298d` 的八行 MENU 面板。显式 Record 会把后续退出检查点切到
玩家刚选择的槽，`--no-save` 则同时屏蔽 Record 和退出写盘，不再只屏蔽后一半。
列表还使用 168×22 选择帧和比例滑块，并从 ITEM `+02/+1d/+1f` 加载真实 CD/AD 物品预览、
`DATA:299a` 类型字图和原 MENU 数字；`298d` 的 PgUp/PgDn/Home/End 也已进入平台无关
输入层，翻页时只移动首个可见项并保留光标所在行，不再用左右箭头伪造整页跳转。
`2907` 滚动条的 MENU 94/95、97/98 端点按键闪帧也随滚动方向显示，frame 99 滑块从
面板 `top+20` 按商余分配定位，不再错误压在顶部箭头上。SDL 键盘直接映射这四键，
手柄则用左右肩键和 X/Y，其他平台可以复用同一语义动作。
选中非空背包格后也不再直接使用或装备：`2d0f` 的左/右/上/下四向 MENU 卡已经恢复为
“使用（装备）/说明/丢弃/炼妖壶”，前三卡始终存在，`SAVE+3f1` 允许时才显示下卡；
未选卡继续使用表 3 调色。说明从 ITEM2 读取，并为 `+05 bit80h` 的 80-byte 扩展物品
显示 `DATA:399a` 八项数值卡；丢弃严格检查 `+05 bit20h`，显示 `DATA:3686/3754`
并经原是/否页确认后压紧 50 个物理槽。`4397` 炼妖壶也已转成平台无关流程：暂时取出
第一件、选择并确认第二件，用 `DATA:2a42` 的 17×17 指针矩阵及 ITEM `+22/+23`
类别/等级求出产物，再检查产物 `+34 <= 主角等级+5`；成功用产物替换第二格并压紧，
取消或 `DATA:36f8` 等级不足则把第一件放回原物理格。产物页使用 `DATA:3922` 的八项
扩展属性，不依赖原 DOS EXE 的段地址。
场景 Cancel 也不再被当作退出程序：它会打开 `2e63` 的原版奇术/物品/系统/状态方向
菱形，四张 MENU 卡按表 3 映射未选项，并在底部保留真实队伍状态卡；右侧物品入口已
接回同一套背包/装备/场景道具逻辑，再次 Cancel 才返回地图。下侧系统入口也恢复
`DATA:39e6` 七行面板、音乐/音效开关、两种五档速度、五槽读取/记录和原版退出确认；
读取会原子替换 SAVE/MAPZ 后重载场景，不再只改状态而沿用旧世界数据。
左侧状态入口也恢复 2634 队员选择与 `DATA:3806` 的 28 行属性/装备滚动页。
上侧奇术入口会从角色 `+6d` 的 50 个 byte 槽读取 `RPG DATA:1dce` 的 20-byte 记录，
显示原八行列表、选中项资源图标/当前点数、费用/五行材料，检查死亡、禁用、点数与材料提示，再通过同一
`FieldActionSystem` 执行目标/全体效果。土地神念法和乘龙念法也分别按地图权限直接
重载当前地点或打开十行目的地列表，取消目的地不会扣除奇术点。选中奇术后还恢复
`2c0b` 的“使用/说明”原 MENU 卡；说明文字来自 `DATE2.EXE` 的 114 项指针目录，
第 4 类符术另有“炼符”上卡，可无点数消耗生成 `ability_id+8c` 的战斗符并压紧背包，
末格占用时显示 `DATA:36e0`。进入奇术或状态的队员选择页也保留原 `211a` 菱形底页，
不再退化成只有四张头像的地图背景。
`12h` 全体恢复会按 `3556..3593` 同时补满生命、体力和奇术资源并清除状态，不再遗漏
第三次针对角色 `+55` 的 `38ab` 调用。
当前代码刻意不把
反编译器生成的伪 C 当作可维护源码。商店购买列表也已换成 `5766` 的五行 MENU 面板、
原始内嵌提示、类型卡和同比例滚动条；购买的金钱/容量/20 件上限反馈及购买确认、出售的
三季度价格提示也都恢复 `DATA:3c1e/3c44/3c54/3c6c/3c80` 与共用 `555a` 是/否页。
不可出售的物品也会显示 `DATA:3c0a`，而非把确认键静默丢弃；这些需确认的底部消息会按
`49d0` 在文字末端循环播放 MENU 149～152 等待标记，购买/出售的即时提示则不会误等。
Opcode 17 还会先令 `DATA:3cd7=1`，在共用双卡上用原 Big5 `B6 52/BD E6` 显示“買/賣”；
卖出后会回到 50 格列表继续选择，空格也走 `DATA:3c0a` 而不会误入购买页，退出子页后再按
当前实体事件字段重载整段商店流程。
装备页使用 `46b1` 的 8×11 全屏面板、ME02 原画、
`DATA:388e/3a60` 槽位/属性字图、原帧光标、角色资源卡和四项真实属性数值；角色禁用、
类别错槽与双手冲突会分别显示原 `DATA:369a/3728/370e` 底部消息，不再无反馈地返回；三条路径的索引页、调色板、原版 RGB、确认/取消及逐层返回均有严格回放锁定。
`3fc1` 每次都从装备第零行开始，而不是预选与物品类别相符的槽位；在背包空格按确认会
跳过物品动作卡，经 `3f53` 选择队员后把现有装备卸入该空格，返回时再由 `3ced` 压紧背包。
不可在场景使用的道具和受地图限制的 28h/29h 传送也恢复 `DATA:3620` 提示。

移植版始终只生成一个游戏可执行文件；原版 EXE 与新模块的合并关系见
[`architecture.md`](architecture.md)。

## 构建

```sh
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

查看原版可执行文件和资源清单：

```sh
./build/src/swd2_rewrite --game game --inspect
./build/src/swd2_rewrite --game game --verify-resources
./build/src/swd2_rewrite --game game --verify-maps
./build/src/swd2_rewrite --game game --verify-battles
./build/src/swd2_rewrite --extract game/MEO.RSK MEO.decoded
./build/src/swd2_rewrite --render game/MEO.RSK 0 MEO-background.ppm
./build/src/swd2_rewrite --game game --render-meo MEO-screen.ppm
./build/src/swd2_rewrite --render-map game/T2/TW-2A TW-2A.ppm
./build/src/swd2_rewrite --render-planar game/DE/DE001 17 DE001-17.ppm
./build/src/swd2_rewrite --game game \
  --save-dir portable-saves --slot 2 \
  --run-script CONFIRM,CONFIRM,CONFIRM,RIGHT,DOWN,QUIT
```

长回放可放在文本文件中，并把动作锁到准确的输入边界，避免动画轮询误消费下一个菜单键：

```sh
./build/src/swd2_rewrite --game game --no-save \
  --run-replay replay.txt --trace-output replay-trace.json \
  --frame-output replay.swd2frames
```

为原版覆盖入口制作短场景证据时，可用 `--start-marker IF` 直接从当前槽进入统一进程内的
FIG 模块；`MT/OC/OM` 和 `ED` 分别直入 RPG 与 DEMO。它只跳过启动调度，不会执行或模拟
任何 DOS EXE，例如五类能力卡基准使用：

```sh
./build/src/swd2_rewrite --game game --save-dir ability-save --no-save \
  --start-marker IF --run-replay scripts/replay-fig-ability-page.txt \
  --trace-output fig.json --frame-output fig.swd2frames
```

语法为 `[WAIT|POLL|TEXT|FRONTEND:]动作[*次数]`，逗号/空白分隔，`#` 到行末为注释。
例如 `POLL:RIGHT*12, WAIT:CONFIRM, TEXT:NONE*4, FRONTEND:QUIT`。严格回放必须消费全部
登记动作；输出记录每个输入前的 SAVE/MAPZ 检查点、逐帧摘要、整段帧链、直接写页、
音频提交、每次延时、统一音画/输入时间线、最终状态摘要和模块切换。
FNV 摘要用于快速确定性比较，最终证据文件本身仍由完成门禁使用 SHA-256 固定。
`--frame-output` 是可选的逐像素证据流：每页保存页类型、320×200 索引像素和完整 VGA
调色板，只有严格回放成功消费全部输入后才写 `DONE` 结尾。可用
`scripts/compare-frame-captures.py` 对原版基准和重写输出做零容差、字节级现场比较。
`scripts/original-fig-if.asm`/`build-original-fig-harness.sh` 则只用于隔离捕获未经修改的
FIG.EXE。当前五类资源卡的确定性 IF 回放已与这些原版捕获在 VGA 第 0..197 行逐 RGB
像素完全一致；底部两行是独立的循环调色板时相，未被伪装成静态容差。物品 51 的
预览、类别、八行名称和数量页则连同底部两行都与原版完整 320×200 RGB 帧一致。
ORC 目录 12 的双怪目标页也已锁定：底层攻击页、两个怪物名和选择条完整帧零差异。

仓库内的短启动检查点会实际走过 `MEO -> RPG(MT/ED) -> DEMO -> RPG(OM)`，装入
`SAVE.DAQ/MAPZ.DAQ` 并执行开场实体后才从世界页退出：

```sh
./build/src/swd2_rewrite --game game --no-save \
  --run-replay scripts/replay-new-game-startup.txt \
  --trace-output new-game-startup.json
./scripts/verify-new-game-startup.py new-game-startup.json
```

它只证明新游戏启动边界，不是完整通关证据。

原版流程可先用隔离的 DOSBox-X 驱动生成可复查的 RGB 录像和抽帧：

```sh
cat > original-input.txt <<'EOF'
# DOSBox-X AUTOTYPE key tokens, in delivery order.
enter kp_2 enter
EOF
./scripts/capture-original-dosbox.py \
  --autotype original-input.txt --time-limit 60 --extract-fps 1 \
  --output /tmp/swd2-original-reference
```

工具把发行文件复制到临时 `C:\\SWD2`、固定 DOS 日期/时间，保存 ZMBV 录像、输入序列、
程序/录像/抽帧 SHA-256 和媒体参数。该录像经过 DOSBox-X VGA DAC 转成 RGB，只能作为
逆向和场景定位参考；manifest 会明确标记 `status=reference_only`，不能冒充像素门要求的
原始索引像素、逐帧 VGA 调色板和完整 `SWD2FRM2` 基准。

需要绕过原版标题而直接复现 `OC` 覆盖入口时，用仓库内的 138-byte harness；只在临时
游戏副本生成它，不改 `game/RPG.EXE`：

```sh
staged_game="$(mktemp -d)"
cp -R game/. "$staged_game/"
# RPG's OC entry reloads the Q checkpoint. Apply any capture-only state
# changes to SAVE.DAQ/MAPZ.DAQ/NAMEQ.DSK, not SAVE.DA1.
./scripts/build-original-rpg-harness.sh "$staged_game/RPGOC.COM"
./scripts/capture-original-dosbox.py --game "$staged_game" \
  --program RPGOC.COM --reference-program RPG.EXE \
  --autotype original-input.txt --output /tmp/swd2-rpg-reference
```

manifest 会分别保存启动 harness 和未修改参考 EXE 的名称及 SHA-256，防止把二者身份
混为一谈。138-byte harness 为了保持既有捕获摘要仍把 `SAVE.DA1` 放在 marker 后面，
但原版 `OC` 分支的 `00b7..00bf` 随即以 `AL=51h` 调用 `4c16`，实际重新载入的是
`SAVE.DAQ/MAPZ.DAQ/NAMEQ.DSK`；准备定向原版场景时必须改 Q 槽。

已登记的 RPG 标题/读档 RGB 抽查可把五张带 SHA-256 的 DOSBox PNG 与烟雾回放的
指定现代帧逐 RGB 像素比较；每张必须 64,000 像素零差异：

```sh
./scripts/compare-dosbox-rgb-frame.py \
  scripts/rpg-opening-rgb-reference.json ORIGINAL_PNG_DIR \
  build/replay-smoke.swd2frames --game game
```

这仍是明确标记的场景抽查，不等于全部 RPG/FIG 的索引像素完成门。
`scripts/rpg-system-menu-rgb-reference.json` 还演示了带 `crop=[x,y,width,height]` 的
完全覆盖面板比较：只比较不会受底层世界动画影响的系统页区域，其余像素不会被误报为已验证。

WASM 构建可在本机已安装的 Chromium/Edge 中执行真实浏览器触摸及 IDBFS 边界测试。runner 会
走到 RPG 世界，只发送一次触摸按下、保持一秒、再发送一次松开，并要求 WASM 每个世界
帧持续收到方向、松开后立即停止；随后用第二个页面完成 IDBFS 写入、提交、重载、逐字节
恢复与清理：

```sh
./scripts/verify-wasm-browser-input.mjs build-wasm/site \
  --output build-wasm/wasm-browser-input-report.json
```

另有独立的 WebKit/IDBFS runner，不把 Chromium 的同一实现重复算成第二个浏览器。
它启动 Playwright WebKit 的实际浏览器进程，要求第一次文档写入并提交探针、自动重载，
第二次文档逐字节恢复、删除并再次提交；它只证明桌面 WebKit 内核的持久化边界，不冒充
品牌 Safari 或物理 iPhone。若 Playwright 未安装在当前 Node 搜索路径，可显式给包目录：

```sh
./scripts/verify-wasm-webkit-idbfs.mjs build-wasm/site \
  --playwright /path/to/node_modules/playwright \
  --reference scripts/wasm-webkit-idbfs-reference.json \
  --output build-wasm/wasm-webkit-idbfs-report.json
```

长期矩阵的本机检查点可重复执行原生进程边界和两种浏览器存储重启。它生成的
`matrix-report.json` 固定为 `status=in_progress`，不会因本机循环通过而伪造最终
`verification/long_run/manifest.json`：

```sh
./scripts/run-long-run-checkpoint.py \
  --playwright /path/to/node_modules/playwright \
  --output verification/long_run/checkpoint-local
./scripts/verify-long-run-checkpoint.py \
  verification/long_run/checkpoint-local
```

2026-08-10 的登记检查点为 250 次原生 MEO→RPG 确定性重放、100 次 Edge IDBFS 重启、
1000 次 WebKit IDBFS 重启；仍缺多小时活动游戏/音频、Windows/Linux、物理手机和手柄，
所以长期门只是从 `pending` 进入 `in_progress`。

该测试使用移动视口模拟，不能替代物理 iOS/Android 设备验收；报告会保留这个限制。

模拟并验证启动器协议。列表中的每一项是相应子模块返回的共享标记：

```sh
./build/src/swd2_rewrite --trace MT,ED,--,IF,OC,--
```

详见 [`reverse-engineering.md`](reverse-engineering.md)。
